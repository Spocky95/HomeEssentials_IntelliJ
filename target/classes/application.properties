spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-homeessentials?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=homeessentials
spring.datasource.password=homeessentials

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update

spring.data.rest.base-path=/api

#allowed.origins=http://localhost:4200 //SSL certificate off
allowed.origins=https://localhost:4200

spring.data.rest.detection-strategy=ANNOTATED

okta.oauth2.client-id=0oagyir3xcfb2g9L65d7
okta.oauth2.issuer=https://dev-29561319.okta.com/oauth2/default

# HTTPS config
# Server web port
server.port=4090
# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=true
# Alias that identifies the key in the key store
server.ssl.key-alias=HomeEssentials
# Keystore location
server.ssl.key-store=classpath:homeessentials-keystore.p12
# Keystore password
server.ssl.key-store-password=secret
# Keystore format
server.ssl.key-store-type=PKCS12