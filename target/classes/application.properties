# Ustawia sterownik JDBC dla bazy danych MySQL.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Ustawia URL po??czenia z baz? danych MySQL.
# Parametry w URL okre?laj? dodatkowe opcje, takie jak wy??czenie SSL, u?ycie Unicode, kodowanie znak�w, zezwolenie na odzyskiwanie klucza publicznego i stref? czasow? serwera.
spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-homeessentials?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Ustawia nazw? u?ytkownika do po??czenia z baz? danych.
spring.datasource.username=homeessentials

# Ustawia has?o do po??czenia z baz? danych.
spring.datasource.password=homeessentials

# Ustawia dialekt Hibernate na MySQL8Dialect, co umo?liwia Hibernate korzystanie z funkcji specyficznych dla MySQL 8.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Ustawia strategi? automatycznego DDL (Data Definition Language) na "update".
# Oznacza to, ?e Hibernate automatycznie tworzy i aktualizuje schemat bazy danych.
spring.jpa.hibernate.ddl-auto=update

# Ustawia podstawow? ?cie?k? dla Spring Data REST na "/api".
# Oznacza to, ?e wszystkie zasoby REST b?d? dost?pne pod ?cie?k? "/api".
spring.data.rest.base-path=/api

allowed.origins=http://localhost:4200

spring.data.rest.detection-strategy=ANNOTATED